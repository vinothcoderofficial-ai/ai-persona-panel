Fixtures created during the build:

- `jittery_gaze.json` / `expected_fixations.json` — S11, the fixation filter test pair.
- `aisle_clip.mp4` — S20, a 20 second phone clip of a real aisle (not committed until recorded).
- `fake_sessions/` — S17, simulator-generated sessions with a known persona mix for the calibration recovery test.
