# API
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlmodel==0.0.22
pydantic==2.10.4
python-multipart==0.0.20
orjson==3.10.13

# sim / analytics
numpy==2.2.1
scipy==1.15.0
pandas==2.2.3
httpx==0.28.1
jsonschema==4.23.0
matplotlib==3.10.0

# seed data
pillow==11.1.0

# vision (S30). CPU only and no deep model: the pipeline reads shelf edges
# and colour runs, which OpenCV does in milliseconds without CUDA. It is in
# the base requirements rather than the optional file because vision/tests
# is in pytest.ini's testpaths - `make test` needs it.
opencv-python-headless==4.10.0.84

# dev
pytest==8.3.4
pytest-asyncio==0.25.2
datamodel-code-generator==0.26.5
